# 自然大调五线谱生成计划

## 目标
创建一个单文件 HTML 应用，使用 VexFlow 生成自然大调（C大调）五线谱。

## 技术栈
- Vue 3.5.31 + Vite
- VexFlow 4.x (五线谱渲染)
- Tailwind CSS (样式)

## 实现步骤

### 1. 项目初始化
- [ ] 创建 Vite + Vue 3 项目
- [ ] 安装 VexFlow 依赖
- [ ] 配置 Tailwind CSS

### 2. 五线谱组件开发
- [ ] 创建 VexFlowScore 组件
- [ ] 实现 C 大调音阶五线谱渲染
- [ ] 支持音符高亮和交互

### 3. 自然大调数据
- [ ] 定义 C 大调音阶音符数据
- [ ] 支持显示八度范围选择
- [ ] 添加升降号处理

### 4. 页面设计
- [ ] 钢琴主题配色（深木色 #2C1810 + 象牙白 #FFFFF0 + 金色 #D4AF37）
- [ ] 五线谱展示区域
- [ ] 音阶播放控制按钮

### 5. 功能扩展
- [ ] 支持其他自然大调（G大调、F大调等）
- [ ] 音符点击播放音效
- [ ] 五线谱导出功能

## 文件结构
```
piano-assistant/
├── index.html
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── src/
│   ├── main.ts
│   ├── App.vue
│   ├── assets/
│   │   └── main.css
│   ├── components/
│   │   └── VexFlowScore.vue
│   └── views/
│       └── ScaleView.vue
```

## 核心代码示例

### VexFlow 渲染音阶
```typescript
import { Renderer, Stave, StaveNote, Voice, Formatter } from 'vexflow'

// C大调音阶音符
const cMajorScale = ['c/4', 'd/4', 'e/4', 'f/4', 'g/4', 'a/4', 'b/4', 'c/5']

// 渲染五线谱
const renderer = new Renderer(container, Renderer.Backends.SVG)
renderer.resize(600, 200)
const context = renderer.getContext()

const stave = new Stave(10, 40, 580)
stave.addClef('treble')
stave.addKeySignature('C')
stave.setContext(context)
stave.draw()
```

## 验收标准
- [ ] 正确显示 C 大调五线谱
- [ ] 音符位置准确
- [ ] 界面符合钢琴主题配色
- [ ] 响应式布局适配
